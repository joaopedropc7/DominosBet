import { SplashScreenView } from '@/features/auth/screens/SplashScreenView';

// Thin route wrapper keeps navigation concerns inside `app/`.
export default function SplashRoute() {
  return <SplashScreenView />;
}
